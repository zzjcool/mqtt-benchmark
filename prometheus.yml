global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'mqtt-benchmark'
    static_configs:
      - targets: ['host.docker.internal:2113']  # 使用 host.docker.internal 访问宿主机
